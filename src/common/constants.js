'use strict';

const BROKER_SUBSCRIBE = '/broker/subscribe/';
const SERVER_UPGRADE = 'upgrade';
const WS_CONNECTION = 'connection';
const WebSocketList = {};

module.exports = {
    BROKER_SUBSCRIBE,
    SERVER_UPGRADE,
    WS_CONNECTION,
    WebSocketList,
};
